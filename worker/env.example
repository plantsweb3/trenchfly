# Keep this in worker/.env (ignored), never in browser code or chat.
# Create a Robinhood Chain MAINNET app: chain ID 4663 (not testnet 46630).
ROBINFLY_RPC_URL=https://robinhood-mainnet.g.alchemy.com/v2/REPLACE_LOCALLY

# Paper mode does not load a signer. Preserve an existing key when editing .env.
# FLY_PRIVATE_KEY is managed separately; do not paste it into this template.
